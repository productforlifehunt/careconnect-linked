# True Care Connect 827 shared backend

make an app called care-connected (logo should be something like C-C), it's a combination of care collaboration apps, e.g. lotsahelpinghands, caringbridge and caregiver search, booking apps, e.g. care.com, sittercity, and also gps tracking app for caregivers to easily locate the users, a feature lotsahelpinghands, caringbridge doesn't have!!! make sure everything is fully functional, implemented, we have all the features, operations for simlar apps, matching or exceeding the current top apps, make sure you have all the operations, e.g. booking, availibility, and everything considered, thoughtfully implemented. and make sure it's a dual platform app, from webapp to mobile app(wrapped in capacitor), the webapp should have full webapp style, with sidebar (and make sure we are only using one header for all desktop pages, and the header should persist on desktop webapp routes, and it's the same header, not a different one!!!) also make sure the app is both for public users and logged in users, because most of the search and booking features are for visitors and should be operated from the homepage or public pages, we shouldn't force auth unless it's absolutely needed. consider the ux for both visitors and logged in users

This project was built with [Lovable](https://lovable.dev).

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/c85459d8-8acf-4fc5-a8e4-d5448f6ae818).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
